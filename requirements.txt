# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database & Auth
supabase>=2.4.0
python-dotenv>=1.0.1

# Data Validation
pydantic[email]>=2.9.0
python-multipart>=0.0.12

# Voice (US-12 — Whisper STT)
# Proveedor local (por defecto): openai/whisper, corre el modelo en la propia
# máquina. Requiere ffmpeg instalado en el sistema. Arrastra torch.
openai-whisper>=20231117
# Proveedor opcional: API hospedada de OpenAI (WHISPER_PROVIDER=openai).
openai>=1.40.0

# Testing
pytest>=8.0.0
httpx>=0.27.0